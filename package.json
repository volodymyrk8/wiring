{
  "name": "wiring",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.app.config.ts && vite build --config vite.home.config.ts && vite build --config vite.profile.config.ts && vite build --config vite.chat.config.ts && vite build --config vite.router.config.ts",
    "dev:client": "vite build --watch",
    "test:router": "node tests/router.test.mjs"
  },
  "dependencies": {
    "preact": "^10.26.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "typescript": "^5.8.2",
    "vite": "^6.2.3"
  }
}
